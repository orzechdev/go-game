

onmessage = function (jebaneGownoKurwa) {
  console.log(jebaneGownoKurwa)
};
// onmessage = function ({currValues, playerColor, val, y, x, depthIteration}) {
//   console.log(currValues)
//   start(currValues, playerColor, val, y, x, depthIteration);
// };

const start = async (currValues) => {
  console.log(currValues)

  postMessage("CHUJ DUPA GÓWNO");
};
